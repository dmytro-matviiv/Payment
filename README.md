# Telegram Bot для моніторингу платежів TRON

Цей бот автоматично відстежує вхідні транзакції на вказану адресу TRON та відправляє повідомлення в Telegram канал.

## 🌐 Деплой на Railway

Для швидкого деплою на Railway дивіться [RAILWAY_DEPLOY.md](RAILWAY_DEPLOY.md)

## 🚀 Швидкий старт

### 1. Встановлення залежностей

```bash
pip install -r requirements.txt
```

### 2. Налаштування

Створіть файл `.env` на основі `.env.example`:

```bash
cp .env.example .env
```

Відредагуйте `.env` файл та встановіть:
- `TELEGRAM_BOT_TOKEN` - токен вашого Telegram бота
- `TELEGRAM_CHANNEL_ID` - ID або username вашого Telegram каналу (наприклад: `@your_channel` або `-1001234567890`)
- `TRONSCAN_API_TOKEN` - токен API Tronscan
- `TRON_ADDRESS` - адреса TRON для моніторингу
- `CHECK_INTERVAL` - інтервал перевірки в секундах (за замовчуванням: 30)

### 3. Отримання Channel ID

**Спосіб 1: Використання утиліти (рекомендовано)**

```bash
python get_channel_id.py
```

**Спосіб 2: Через API**

1. Додайте бота в канал як адміністратора
2. Надішліть будь-яке повідомлення в канал
3. Перейдіть за посиланням: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
4. Знайдіть `chat.id` в відповіді - це і є ваш Channel ID

**Спосіб 3: Через бота**

Використайте бота [@userinfobot](https://t.me/userinfobot) - перешліть повідомлення з каналу боту, і він покаже ID.

**Примітка:** Для публічних каналів можна використати username каналу (наприклад: `@your_channel`) замість числового ID.

### 4. Запуск бота

```bash
python bot.py
```

## 📋 Функціонал

- ✅ Автоматичний моніторинг вхідних транзакцій на вказану адресу TRON
- ✅ Відправка повідомлень в Telegram канал при отриманні нової оплати
- ✅ Детальна інформація про транзакцію (сума, адреси, час, посилання)
- ✅ Захист від дублікатів повідомлень
- ✅ Налаштовуваний інтервал перевірки

## 📝 Формат повідомлення

Кожне повідомлення містить:
- 💰 Сума транзакції
- 📥 Адреса отримання
- 📤 Адреса відправника
- 🕐 Час транзакції
- 🔗 Посилання на транзакцію в Tronscan

## ⚙️ Налаштування

### Зміна інтервалу перевірки

Відредагуйте `CHECK_INTERVAL` в файлі `.env` або `config.py`. Менший інтервал = частіші перевірки, але більше навантаження на API.

### Моніторинг іншої адреси

Змініть `TRON_ADDRESS` в файлі `.env`.

## 🔧 Технічні деталі

- **Мова**: Python 3.7+
- **Бібліотеки**: 
  - `python-telegram-bot` - для роботи з Telegram API
  - `requests` - для HTTP запитів до Tronscan API
  - `python-dotenv` - для роботи з змінними середовища

## 📌 Примітки

- Переконайтеся, що бот має права на відправку повідомлень у канал
- Tronscan API має обмеження на кількість запитів - не встановлюйте занадто малий інтервал
- Бот зберігає історію оброблених транзакцій у файлі `processed_transactions.json` - це запобігає дублюванню повідомлень після перезапуску
- Для публічних каналів можна використати username (наприклад: `@your_channel`) замість числового ID

## 🐛 Вирішення проблем

### Бот не відправляє повідомлення

1. Перевірте, чи бот додано в канал як адміністратор
2. Перевірте правильність Channel ID
3. Перевірте токен бота

### Помилки API Tronscan

1. Перевірте правильність API токену
2. Перевірте, чи не перевищено ліміт запитів
3. Перевірте правильність адреси TRON

## 📞 Підтримка

При виникненні проблем перевірте логи бота - вони містять детальну інформацію про помилки.

